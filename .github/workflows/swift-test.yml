name: Swift Test

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  swift-test:
    runs-on: macos-26

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Run test suite
        run: swift test
